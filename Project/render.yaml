services:
  - type: web
    name: django-app
    env: python
    buildCommand: cd Project && pip install -r ../requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate
    startCommand: cd Project && gunicorn Project.wsgi:application
    envVars:
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "django-project-q0rp.onrender.com,localhost,127.0.0.1"
